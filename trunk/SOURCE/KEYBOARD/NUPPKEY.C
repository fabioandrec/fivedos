#include <ClipApi.h>

BYTE _upcase( BYTE );

//---------------------------------------------------------------------------//

CLIPPER nUpperKey( void ) // ( nKey ) > nUpperKey
{
    _retni( _upcase( _parni( 1 ) ) );
}

//---------------------------------------------------------------------------//
