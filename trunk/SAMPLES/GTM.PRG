

function main()
    local n, t

    n := GetTimer()
    t := Seconds()

    while inkey() != 27
        // ? n - ( n := GetTimer() ), t := Seconds(), GetTimerbase()
        ? GetTimerbase(), GetTimerrr()
    end

return nil
